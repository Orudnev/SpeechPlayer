import compareResult from './components/CompareResult';

export default function test(){
    let res = compareResult("Script tag without async or defer attributes, blocks HTML parsing the script is fetched and executed immediately."
    ,"kryptek without async or defer prepdeck attributes");
    let s=1 ;
}